const fs = require("fs");
const input = fs.readFileSync(0, "utf8").trim().split(/\s+/).map(Number);

let a = input[0];
let b = input[1];
let c = input[2];

// Prints a
console.log(a);

// Prints b
// console.log(b);

// Prints c
console.log(c);